# Build-tool dependencies (labs themselves run on Colab and manage their own deps)
python-pptx>=1.0
pyyaml>=6.0
pillow>=10.0
# Slides are authored as YAML specs (slides/spec/) and built to native, editable
# PowerPoint with tools/spec2pptx.py. No browser/Playwright needed.
