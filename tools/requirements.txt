# Build-tool dependencies (labs themselves run on Colab and manage their own deps)
python-pptx>=1.0
pyyaml>=6.0
pillow>=10.0
# Decks are hand-edited PowerPoint in slides/pptx/ (the source of truth) and
# scripted with python-pptx. No browser/Playwright needed.
