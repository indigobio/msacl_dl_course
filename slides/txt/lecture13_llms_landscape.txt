# lecture13_llms_landscape.pptx — 33 slides
# (generated by tools/pptx2txt.py; do not hand-edit)

--- slide 1 · layout: Blank ---
MSACL · DS301 DEEP LEARNING · SEGMENT 5 · LECTURE 13
Large language models, and the DL-in-MS landscape
LLMs demystified in pictures · reinforcement learning, touched base · prompting vs RAG vs fine-tuning · the landmark-tool tour
NOTES:
  Tuesday morning, Segment 5, the frontier hour. Two jobs. First, DEMYSTIFY large language models in pictures: next-token predictors, trained in three stages (pretraining → SFT → preference tuning/RLHF), with hallucination as a failure mode we DESIGN AROUND; one light numeric beat, a softmax over three candidate-token logits. Second, the LANDSCAPE TOUR: five landmark tools, each in the same frame — problem → the architecture you now know → impact — and, as of 2026-09-11, each with ITS PAPER: a full citation with DOI and that paper's headline experimental result on the slide (Casanovo 0.81 average precision on the nine-species benchmark; Prosit's 550,000 peptides / 21 M spectra and >10× lower FDR; DIA-NN's >35,000 precursors off a half-hour gradient; DRIAMS's 0.80/0.74/0.74 AUROC and the 63-patient review; AlphaFold's 0.96 Å versus 2.8 Å at CASP14). Between them sits REINFORCEMENT LEARNING, now taught properly rather than touched base and on the CLASSIC examples — a gridworld, AlphaGo, a two-armed bandit, an aliased corridor — across eleven slides: the loop, state & policy, the Markov requirement, AlphaGo, delayed reward and the discounted return, explore vs exploit, and then RLHF in three slides — the mapping, the machinery (preference pairs → reward model → tuning the policy), and the honest account of reward hacking, sycophancy and why it does NOT fix hallucination. Still no MDPs and no policy gradients. That block pays off twice: RLHF is 'RL where the reward comes from human preferences', and Lecture 14's agents run the same loop. We also cover using an LLM without training one (prompting vs RAG vs fine-tuning). Hand out Quiz 13 blank now — FIVE items, filled across the hour: Q1 formulate an RL problem from your own lab (open-ended, and where the RL block comes home to mass spec), Q2 prompting vs RAG vs fine-tuning, Q3 spot the untrustworthy claim, Q4 explore vs exploit, Q5 the discounted return. NOTE ON TIME: with the deepened RL block the taught content runs ~16 minutes past the hour by design — the RL-mechanics slides are marked OPTIONAL DEPTH in their notes with a cut order. Do not buy the time out of the tour.

--- slide 2 · layout: Blank ---
LECTURE 13 · WHERE THIS HOUR GOES
Four stops on the frontier
•  LLMs demystified — next-token prediction, the 3-stage training pipeline, scale, hallucination   · 14 min
•  Reinforcement learning, properly — the mechanics, then RLHF opened up   · 30 min
•  Using an LLM without training one — prompting vs RAG (opened up) vs fine-tuning   · 12 min
•  The landscape tour — five landmark tools, each with its paper: problem → architecture → result   · 16 min
NOTES:
  Signpost the four stops. The only arithmetic all hour is one softmax (the do-it-together in the first stop) and, in the RL stop, one ε/K probability and one discounted return — all multiply-and-divide, so reassure the room early. The heart of the hour is the last stop, the landscape tour, where everything from Lectures 5–11 comes home: a transformer that translates a spectrum into a peptide, a 1D CNN that calls antibiotic resistance, attention that folds a protein — each now with its paper and its headline number on the slide. Aim for a hands-on beat every ~8–10 minutes: the softmax do-it-together, the discounted-return do-it-together, the tour's spoken place-a-new-tool beat, and the four you-dos (hallucination → Q3, explore vs exploit → Q4, formulate your own RL problem → Q1, prompting/RAG/fine-tuning → Q2). TIMING (revised 2026-09-14): the RL stop is now eleven slides / ~30 min — the mechanics on classic examples, plus RLHF opened up over three slides — so the taught content runs roughly 16 minutes past the hour BY DESIGN. Decide live what to drop, in this order. CUT FIRST: explore vs exploit + its you-do (~6 min, costs Quiz 13 Q4). CUT SECOND: delayed reward (~3 min, costs Q5). CUT THIRD, and only if you must: 'RLHF, opened up' (~3 min) — but keep 'RLHF, honestly' even then, because reward hacking, sycophancy and 'it does not fix hallucination' are the things this audience actually has to know, and they close the loop on the hallucination guardrail from the first stop. ALWAYS KEEP: the anatomy slide (STATE and POLICY, which everything later leans on) and the Markov slide (~2 min each; Q1(f) asks for Markov). Do NOT buy the time out of the landscape tour: that is the payoff of the whole course.

--- slide 3 · layout: Blank ---
LLMS DEMYSTIFIED · 1 OF 6 · THE CORE IDEA
An LLM predicts the next token
[image: Picture 4]
MASS SPEC · Strip away the mystique: a language model reads the words so far and outputs a probability for EVERY token in its vocabulary — then it picks (usually) the most likely one and appends it. "The internal standard was spiked into each ___" → "sample". Generate text by doing this over and over.
NOTES:
  Demystify the whole thing in one picture. A large language model is a next-token predictor: feed it the words so far — here an ordinary English sentence out of a lab report, "The internal standard was spiked into each ___" — and it returns a probability distribution over the ENTIRE vocabulary for what comes next; pick a word, append it, and repeat — that loop is how it writes a sentence, a summary, or a whole paragraph. The bars show the model favoring "sample" (0.55) over "vial", "tube", and the long tail of every other word in the language. Nothing here is magic — the tokens are the same word-pieces we tokenized in Lecture 7, and the probabilities are the softmax over scores we already know from Lecture 4 and used in attention (Lecture 7). Next slide we compute one of those probability sets by hand so "next-token prediction" is concrete, not a slogan. (This is also the decoder-only generation you met in Lecture 8 — and, as we foreshadowed there, an LLM is TRAINED on exactly this next-token game over a huge text corpus. Sequence models over OUR data — a peptide extended one residue at a time — are the same game in another vocabulary, and the landscape tour at the end of the hour shows those.)

--- slide 4 · layout: Blank ---
LLMS DEMYSTIFIED · 2 OF 6 · DO-IT-TOGETHER
Do it together: softmax the next-token scores
[image: Picture 4]
CHECKPOINT · Do-it-together (fill it in with me): three candidate next words — "sample", "vial", "tube" — carry logits 2, 1, 0. softmax turns them into probabilities — e² =7.39, e¹ =2.72, e⁰ =1.00, sum 11.11 → 0.665, 0.245, 0.090 (they sum to 1). Argmax → predicted next word = "sample". That is next-token prediction, exactly.
NOTES:
  The hour’s one numeric beat (rule 6), done together cell by cell — a reuse of the Lecture 4/7 softmax so it’s familiar. Continuing the same sentence, "… spiked into each ___", three candidate next words "sample", "vial", "tube" carry logits 2, 1, 0. Softmax(z)ᵢ = e^{zᵢ} / Σⱼ e^{zⱼ}. Exponentiate: e²=7.389, e¹=2.718, e⁰=1.000; the sum is 7.389+2.718+1.000 = 11.107. Divide each by the sum: 7.389/11.107 = 0.665, 2.718/11.107 = 0.245, 1.000/11.107 = 0.090 — and they sum to 1.000, as probabilities must. The argmax is the largest, 0.665, so the predicted next word is "sample". Emphasize the intuition: a bigger logit becomes a bigger probability, and the model would (greedily) emit "sample". This is exactly what happens billions of times as an LLM writes — and the numbers are identical to the figure and notes. Not a quiz item; it just makes the next-token idea tangible before we zoom out to how the whole model is trained.

--- slide 5 · layout: Blank ---
LLMS DEMYSTIFIED · 3 OF 6 · THE TRAINING PIPELINE
Three training stages, each in one sentence
[image: Picture 4]
1) PRETRAINING — predict the next token on a huge text corpus → learns language & facts. 2) SUPERVISED FINE-TUNING (SFT) — train on curated instruction→answer pairs → learns to follow instructions. 3) PREFERENCE TUNING (RLHF) — humans rank answers, tune toward the preferred → learns to be helpful & safe.
NOTES:
  How a chatbot is built, three stages, one sentence each. STAGE 1, PRETRAINING: the next-token game from the last two slides, run on a massive text corpus — this is where the model absorbs grammar, facts, and reasoning patterns, but it only knows how to CONTINUE text, not to answer you. STAGE 2, SUPERVISED FINE-TUNING (SFT): keep training, now on curated examples of instruction → good answer, so the model learns to follow instructions instead of just autocompleting. STAGE 3, PREFERENCE TUNING (RLHF): humans compare answers and mark which they prefer, and the model is tuned toward the preferred ones — this is what makes it feel helpful, polite, and safe. We will unpack that third stage after a quick detour through reinforcement learning, because "tune toward what humans prefer" IS reinforcement learning. Base model → instruction-follower → assistant.

--- slide 6 · layout: Blank ---
LLMS DEMYSTIFIED · 4 OF 6 · WHY IT WORKS
Scale is the engine
[image: Picture 4]
Why did this suddenly work? SCALE. More data + more parameters + more compute, and capabilities that weren't programmed in appear — grammar, then reasoning and coding, then broad 'emergent' skills. Same next-token objective the whole way up; the size is what changed.
NOTES:
  The honest answer to "why now?" is scale. The training objective never changed — it is still next-token prediction — but as models grew from ~100M to ~10B to ~1T parameters (trained on correspondingly more data and compute), qualitatively new abilities appeared: first fluent grammar, then multi-step reasoning and code, then the broad, surprising "emergent" skills that make today's assistants useful. The lesson for a lab audience is twofold: these abilities are a product of scale, not hand-coded rules (which is also why they're hard to fully predict or guarantee), and you almost never train one yourself — you USE a pretrained one (stop three of the hour). But scale also brings a failure mode we must plan for, next.

--- slide 7 · layout: Blank ---
LLMS DEMYSTIFIED · 5 OF 6 · THE FAILURE MODE · I-DO
Hallucination: fluent, confident — and sometimes wrong
[image: Picture 4]
MASS SPEC · An LLM optimizes for plausible next tokens, not truth — so it can produce a fluent, confident answer that is simply WRONG, like an invented citation. In a clinical or lab setting that is dangerous. The guardrail: GROUND answers in real source documents (RAG) and require a human to verify every citation/number before it ships.
NOTES:
  The critical caveat for anyone deploying this in a lab. Because a model is trained to produce PLAUSIBLE next tokens, not verified truth, it will sometimes generate something fluent and confident that is flatly wrong — an invented reference, a made-up limit of detection, a plausible-looking but fabricated number. This is "hallucination," and it is not a bug you can fully patch out; it is a property of the objective, so you DESIGN AROUND it. Two guardrails we lean on: (1) grounding — retrieve real source documents and make the model answer from them (that's RAG, two stops from now), so answers are anchored to your actual SOPs/records; and (2) a human-in-the-loop check that verifies every citation and number before it reaches a report. The takeaway: treat LLM output as a draft to verify, not an oracle. Next slide the room states a guardrail themselves (Quiz 13 Q3).

--- slide 8 · layout: Blank ---
LLMS DEMYSTIFIED · 6 OF 6 · YOUR TURN (YOU-DO = Q3)
Your turn — which claims can't you trust, and why?
[image: Picture 4]
CHECKPOINT · Your turn → Quiz 13, Q3. An LLM answers 'It's 48 nmol/L — see Nguyen et al., Clin. Chem. 2021.' (a) Which parts can you NOT trust without checking a source? (b) Why did the model state them so confidently? Worked answer in the notes.
NOTES:
  YOU-DO = Quiz 13 Q3 (SPOT the untrustworthy claims and explain the mechanism — not 'name the guardrail from the last slide'). (a) BOTH specifics must be verified: the NUMBER (48 nmol/L) and the CITATION (Nguyen et al., Clin. Chem. 2021). You cannot tell from the fluent wording whether either is real — any specific fact an LLM emits without a cited source is unverified by construction, so both are suspect. (b) Because the model is trained to produce PLAUSIBLE next tokens, not to check truth, a fabricated number or paper comes out with the same confidence as a real one. The lesson: treat every LLM-produced specific (number, citation, dose) as a draft to verify; the guardrails (ground in RAG'd sources, human sign-off) exist precisely because you can't trust fluency. Take one answer from the floor, then have them fill Q3.

--- slide 9 · layout: Blank ---
LECTURE 13 · PART TWO
Reinforcement learning
the loop, state & policy, the Markov requirement, explore vs exploit — then RLHF, opened up and told honestly
NOTES:
  A detour so stage three of the pipeline (RLHF) makes sense, so Lecture 14's agents have the vocabulary, and — at the instructor's request (2026-09-11) — so the room actually understands RL rather than recognising a diagram. Two design rules. (1) It stays intuitive: no Markov decision-process formalism, no policy gradients; the only arithmetic is one division (ε/K) and one square (γ²). (2) It is taught on the CLASSIC examples — a gridworld, AlphaGo, a two-armed bandit, an aliased corridor — deliberately NOT on mass-spec ones, because the mechanics land more cleanly when they are not entangled with chemistry. The MS anchor is saved for the closing you-do, where the room formulates a problem from their OWN lab (Quiz 13 Q1). Eleven slides in a deliberate order. THE LOOP gives the four words. THE ANATOMY adds the two it leaves out, STATE and POLICY, on a gridworld episode, and makes 'there is no label, only a reward' concrete. THE REQUIREMENT is the Markov property — the state must be enough on its own — with the gridworld cell as the good case and a wall-sensing agent in a corridor as the broken one, where two cells look identical but need opposite moves. ALPHAGO is the famous example and sets up the next problem. DELAYED REWARD is credit assignment and the discounted return. EXPLORE vs EXPLOIT is the bandit, ε-greedy worked exactly, plus its you-do. Then RLHF gets three slides rather than one (added 2026-09-14): it snaps the loop onto language — the reward is a human preference — then OPENED UP, the three steps it is really built from (preference pairs → a reward model → tuning the policy on a leash), then HONESTLY, what that buys (a base model merely continues your text; the tuned one answers) and what it breaks (reward hacking, sycophancy, and no fix for hallucination). Finally the closing you-do sends everyone to their own bench.

--- slide 10 · layout: Blank ---
RL · 1 OF 11 · THE LOOP · I-DO
One loop: agent, environment, action, reward
[image: Picture 4]
The whole of reinforcement learning in one diagram. The AGENT takes an ACTION in an ENVIRONMENT; the environment returns a REWARD (and a new state). The agent has no labeled 'right answers' — it learns purely by trial and feedback, keeping the actions that earn more reward.
NOTES:
  Reinforcement learning in one picture, no math. Four words: the AGENT (the learner) takes an ACTION; the ENVIRONMENT (the world it acts in) responds with a REWARD — a single number saying better or worse — and a new state; the agent then acts again, gradually favoring actions that earn more reward. The defining feature versus everything earlier in the course: there are NO labeled examples of the correct action. Supervised learning is shown the answer; an RL agent must DISCOVER good actions by trying them and reading the feedback. That is "learn by trial and feedback." Hold these four roles — agent, environment, action, reward — because the room will label them on a lab scenario in two slides (Quiz 13 Q2), and because RLHF is exactly this loop with a human-supplied reward.

--- slide 11 · layout: Blank ---
RL · 2 OF 11 · THE ANATOMY
State and policy, on the classic gridworld
[image: Picture 4]
MASS SPEC · Two more words complete the loop. STATE = where you are — which CELL. POLICY = the rule turning state into action, and the policy IS the network you train. Three moves, ↑ → →, paying 0, 0, +1 at the goal. Nobody said which move was “correct” — no label, only a reward.
NOTES:
  OPTIONAL DEPTH (2026-09-11) — the first of the RL-mechanics slides. Taught on the GRIDWORLD, the canonical RL example (instructor, 2026-09-11: use classic RL examples, not mass-spec ones — the mechanics land better when they are not entangled with chemistry, and the MS anchor moves to the closing you-do where the room formulates its own). Read the grid left panel first: a 3×3 world, the agent (amber dot) starts in the middle-left cell, the goal is top-right, and the reward is 0 on every move and +1 only on arriving. Then the two new words. STATE: where the agent is — here literally which of the nine cells, s1…s9. POLICY (π): the rule that maps a state to an action. Say the important part slowly — when somebody says they 'trained an RL agent', the POLICY is the network whose weights changed; the environment and the reward are the world it learned against. A nice concrete image: the policy is the arrow you would draw in every cell of this grid. Then walk the episode table: move 1 from the start cell, ↑, reward 0; move 2 from the top-left, →, reward 0; move 3 from the top-middle, →, onto the goal, reward +1. TWO PUNCHLINES. First, and it is why RL is a different category from Lectures 1–11: at no point did anyone say which move was correct. There is no label — only a reward. Supervised learning is shown the answer; an RL agent has to infer it from consequences. Second, set up the next-but-one slide: the only reward in the whole episode arrived at the END, so how does move 1 ever get credit? Hold that. The very next slide is the requirement that makes any of this work — the Markov property.

--- slide 12 · layout: Blank ---
RL · 3 OF 11 · THE REQUIREMENT
The Markov property: is your state enough?
[image: Picture 4]
A state is MARKOV when the future depends only on it, not on the path that got you there. The gridworld CELL qualifies. But an agent sensing only “wall left? wall right?” reads corridor cells 2 and 4 identically, yet must move opposite ways. Markov is a property of the STATE YOU CHOOSE, not of the world.
NOTES:
  NEW (instructor, 2026-09-11). Intuitive only: the Markov property in words and pictures, no MDP formalism, no transition matrices. THE DEFINITION, one sentence to say aloud: a state is MARKOV if knowing it is enough — the future depends on where you are now, not on how you got here. LEFT, the good case. Two agents reach the same gridworld cell by different routes, one short, one long. Ask the room: does the better next move differ between them? No. Same cell, same best move, same expected future — the history adds nothing, so the CELL is a Markov state. RIGHT, the failure, and it is the classic one: PERCEPTUAL ALIASING. Put an agent in a five-cell corridor with the goal in the middle, and let it sense only whether there is a wall immediately to its left and immediately to its right. From cell 2 it reads 'open | open'. From cell 4 it reads 'open | open' — the identical observation. But from cell 2 the goal lies to the RIGHT and from cell 4 it lies to the LEFT, so the same reading demands opposite actions. No policy can be right in both places, because the agent cannot tell them apart. That is what a non-Markov state costs you. THE POINT TO LAND, and it is the line along the bottom of the slide: Markov is a property of the STATE YOU CHOOSE, not of the world. The corridor itself is perfectly well behaved — the true cell IS Markov. What is broken is the impoverished observation you handed the agent. So the fix is always to enrich the STATE, never to swap the algorithm: give it its position, let it remember which way it came, or carry a short history. The same move shows up everywhere — add a rate of change when a single reading is ambiguous, or stack the last few frames, which is exactly what the Atari DQN work does, since one video frame cannot tell you which way the ball is travelling even though the game's full state is Markov. WHY IT MATTERS TO THEM, worth 30 seconds: every tool on the following slides — the policy, the discounted return, the value of a state — quietly assumes the state is Markov. When an RL project misbehaves the first question is not 'which algorithm?' but 'is my state actually sufficient?'. In a lab, 'the current instrument reading' is usually NOT enough; 'the reading, plus how fast it is drifting, plus where in the run we are' usually is. They need exactly this judgement in the closing you-do, Quiz 13 Q1(f). Cheap at ~2 minutes and I would keep it — it is the idea that stops RL from looking like magic. A NOTE ON THE EXAMPLE, so nobody swaps it back: do NOT use cart-pole as the failure case. Cart-pole's physics state — position, velocity, angle, angular velocity — is perfectly Markov, so it is a bad illustration of a broken state. Perceptual aliasing in a corridor is the honest one, because there the impoverished OBSERVATION really is insufficient while the underlying world is fine.

--- slide 13 · layout: Blank ---
RL · 4 OF 11 · THE FAMOUS EXAMPLE
AlphaGo: trial, feedback, improvement
[image: Picture 4]
Go board · photo: Xchen27, Wikimedia Commons, CC BY-SA 3.0 (reused course asset)
AlphaGo learned Go with this exact loop: ACTION = place a stone, ENVIRONMENT = the game, REWARD = win or lose. Play millions of games, keep the moves that lead to wins, and it beat the world champion — no human ever labeled the 'right' move. Trial → feedback → improvement, at scale.
NOTES:
  One slide, the canonical RL story, mapped straight onto the loop. AlphaGo (DeepMind, 2016) became superhuman at Go with reinforcement learning: the ACTION is placing a stone, the ENVIRONMENT is the game of Go, and the REWARD is simply winning or losing at the end. By playing millions of games — including against itself — and reinforcing the moves that tended to lead to a win, it discovered strategies no human taught it and beat world champion Lee Sedol. The point for us is not Go; it's the pattern: trial → feedback → improvement, no labeled "correct" moves, just a reward signal and enormous practice. That same pattern, with a human preference standing in for "win or lose," is how RLHF tunes an LLM — next slide. (This is a reused, properly-licensed course image, credited in the caption.)

--- slide 14 · layout: Blank ---
RL · 5 OF 11 · DELAYED REWARD · DO-IT-TOGETHER
The reward arrives late — so discount it back
[image: Picture 4]
CHECKPOINT · AlphaGo's win lands 200 moves after the move that earned it — the CREDIT-ASSIGNMENT problem. Fix: score a step by the rewards that FOLLOW it, discounted: G = r₁ + γ·r₂ + γ²·r₃. Together: the gridworld moves paying 0, 0, 1 at γ = 0.9 → move 1 earns 0.81. Your turn → Quiz 13, Q5.
NOTES:
  OPTIONAL DEPTH (2026-09-11) — DO-IT-TOGETHER, ~3 minutes, and it lands right after AlphaGo because AlphaGo is the problem in its purest form. THE PROBLEM first. In the loop we drew, a reward follows each action. Real tasks are not like that: AlphaGo plays ~200 moves and gets ONE number at the end, win or lose. Which move earned it? That is CREDIT ASSIGNMENT, the single hardest thing about RL. Our version, on the left, is the SAME gridworld episode from the anatomy slide — three moves, ↑ then → then →, paying 0, 0 and finally +1 on reaching the goal. Moves 1 and 2 scored nothing, yet they clearly set up the win. THE FIX, and this is the arithmetic to do together. Stop scoring a step by its own reward and score it by the RETURN — the rewards that FOLLOW it, each discounted one notch per step: G = r₁ + γ·r₂ + γ²·r₃. Put γ = 0.9 on the board and compute the three credits with the room. Move 1: 0 + 0.9×0 + 0.9²×1; ask what 0.9² is (0.81); so G = 0.81. Move 2: 0 + 0.9×1 = 0.90. Move 3: 1.00. Read the pattern aloud — every move now gets credit for the eventual win, and the ones nearer the payoff get more of it. WHAT γ MEANS, the dial along the bottom: γ = 0 gives move 1 a credit of 0.00, a myopic agent that values only the reward in front of it; γ = 0.9 gives 0.81; γ = 0.99 gives 0.98, a patient agent for which a distant win counts almost fully. Choosing γ is choosing how far ahead the agent cares, and for a long episode — a method development, or a game of Go — you want it near 1. Quiz 13 Q5 is this same episode at γ = 0.5 → 0.5² × 1 = 0.25, plus one sentence on what a small γ does; point them at the sheet now. If you are cutting for time, cut this slide SECOND, after the explore/exploit pair.

--- slide 15 · layout: Blank ---
RL · 6 OF 11 · EXPLORE vs EXPLOIT · I-DO
Explore or exploit — every single cycle
[image: Picture 4]
MASS SPEC · The classic two-armed bandit. Machine A: played 20 times, mean 7.0. Machine B: played ONCE, mean 5.0. ε-greedy — with probability ε play at random, else play the best average — so P(a specific non-greedy action) = ε/K. With ε = 0.1, K = 2: exploit plays A; B comes up 0.1/2 = 0.05 of the time.
NOTES:
  OPTIONAL DEPTH (2026-09-11) — I-DO, ~3 minutes, on the MULTI-ARMED BANDIT, which is where the words 'explore' and 'exploit' come from and the canonical example in every RL text (instructor, 2026-09-11: use the classic examples). THE TENSION: every cycle the agent chooses between EXPLOITING what it already knows works and EXPLORING something it has barely tried. Set it up on the left: machine A has been played 20 times for 140 coins, so its mean is 140/20 = 7.0 — do that division aloud. Machine B has been played exactly ONCE, for 5 coins, mean 5/1 = 5.0. Ask the room which they would play next. Most say A, and they are right that A LOOKS better — press on why B's 5.0 is nearly meaningless. One pull. One unlucky spin and that average is noise. THE RULE, right panel, and it is the line they need: ε-greedy. With probability ε pick uniformly at random among all K actions; otherwise take the highest average so far. So the probability of taking one SPECIFIC non-greedy action is ε/K. Work it: ε = 0.1, K = 2, so P(B) = 0.1/2 = 0.05, about 1 cycle in 20. Ask for the greedy pick first (A, mean 7.0), then the 0.05. THE PAYOFF: over 200 cycles B gets tried roughly 10 times, and its TRUE mean turns out to be 8.0 — better than A. Pure greed would have locked onto A after one bad pull and never found out. The everyday version lands well: if you only ever order the dish you know, you never discover a better one. Two notes worth giving: ε is usually ANNEALED — explore early, exploit late, the same shape as Lecture 4's learning-rate decay and Lecture 10's curriculum — and this is why an RL agent must be allowed to look wrong for a while. Next slide the room runs the same rule on different numbers (Quiz 13 Q4).

--- slide 16 · layout: Blank ---
RL · 7 OF 11 · YOUR TURN (YOU-DO = Q4)
Your turn — explore or exploit?
[image: Picture 4]
CHECKPOINT · Your turn → Quiz 13, Q4. Same rule, two routes to work. Route A: driven 30 times, mean 6.0. Route B: driven twice, mean 4.0. ε = 0.2, K = 2. (a) Which route does GREEDY take? (b) P(this trip takes B)? (c) One sentence: why is ALWAYS-greedy a mistake here? Worked answer in the notes.
NOTES:
  YOU-DO = Quiz 13 Q4, the same shape as the bandit I-do with different numbers and a second classic framing — two routes to work, the everyday bandit. Answers, identical to the Q4 key: (a) GREEDY takes ROUTE A — its average is 180/30 = 6.0 against B's 8/2 = 4.0, and greedy means 'highest average so far', full stop. (b) P(B) = ε/K = 0.2/2 = 0.10, about one trip in ten; greedy never picks B, so the only route to B is the random draw. Watch for the common slip of answering 0.2 — that is the probability of exploring AT ALL, and half those draws land back on A. (c) Any equivalent of: route B has been driven only twice, so its 4.0 is nearly meaningless — two bad days of traffic would produce it — and an always-greedy driver would take A forever and never gather the evidence that would settle which is faster. Take one answer for (a) and (b) from the floor, one sentence for (c), then have them fill Q4. If you skipped the explore/exploit pair, tell the room to leave Q4 blank.

--- slide 17 · layout: Blank ---
RL · 8 OF 11 · BACK TO LLMS
RLHF = RL where the reward is a human preference
[image: Picture 4]
MASS SPEC · Now stage 3 of the pipeline makes sense. AGENT = the LLM, ACTION = write an answer, ENVIRONMENT = the prompt + a human rater, REWARD = how much a human PREFERS the answer. Same trial-and-feedback loop as AlphaGo — the only new thing is that people supply the reward. That's RLHF.
NOTES:
  Close the loop back to the pipeline. Reinforcement Learning from Human Feedback (RLHF) is the agent–environment–reward loop with the roles filled in for language: the AGENT is the LLM being tuned; its ACTION is to write an answer to a prompt; the ENVIRONMENT is the prompt plus a human rater; and the REWARD is how strongly a human prefers that answer over the alternatives. The model is nudged, over many rounds, toward answers people prefer — which is what turns a raw instruction-follower into a helpful, polite, safe assistant. No new machinery beyond the loop we just drew; that is why RLHF is 'RL where the reward comes from human preferences', and it completes the three-stage training pipeline from the start of the hour. Leave them with the obvious objection rather than answering it — surely nobody can grade every answer by hand? — because the next slide (added 2026-09-14) opens the box and shows the reward MODEL that solves exactly that, and the slide after it is the honest account of what the whole procedure breaks.

--- slide 18 · layout: Blank ---
RL · 9 OF 11 · RLHF, OPENED UP
How RLHF is actually built — three steps
[image: Picture 4]
MASS SPEC · Where does the reward come from? (1) COLLECT PREFERENCES — same prompt, two answers, a human says which is better. (2) TRAIN A REWARD MODEL — it learns to score answers the way the raters did, from comparisons alone. (3) TUNE THE POLICY — the LLM writes, the reward model scores, the LLM is nudged higher.
NOTES:
  OPTIONAL DEPTH (instructor, 2026-09-14) — the previous slide said RLHF is the RL loop with a human reward. This one opens the box, because 'a human supplies the reward' raises an obvious question the room will be thinking: surely you cannot have a person grade every answer? Three steps, left to right. STEP 1, COLLECT PREFERENCES. Take one prompt and sample two answers from the model, then ask a human which is better. Our example is a question any of them could be asked: 'QC failed on this run — report the cortisol result?' Answer A refuses and says repeat the QC; answer B says go ahead if the value looks reasonable. The rater picks A. Note what the rater did NOT do — they did not write the ideal answer, and they did not give a score out of ten. Comparing two things is far easier and far more consistent than scoring one thing, which is exactly why RLHF is built on comparisons. Do this thousands of times and you have a pile of (prompt, better, worse) triples. STEP 2, TRAIN A REWARD MODEL. Now train a SECOND network that takes a prompt and an answer and emits one number. It is trained on the pairs, so all it learns is an ordering — score(A) greater than score(B) — never an absolute 'correct' score. This is ordinary supervised learning, on human preferences. And here is why the step exists: once you have it, the reward is AUTOMATIC. No human needs to sit in the loop for every answer any more; the reward model stands in for them, millions of times. STEP 3, TUNE THE POLICY. Now the RL loop from earlier runs for real, and use the vocabulary deliberately: the LLM IS the policy — the exact word from the anatomy slide, the network whose weights change. It writes an answer, the reward model scores it, and the LLM is nudged toward answers that score higher. Write, score, nudge, repeat. AND THE LEASH, which is the part people leave out: you also penalise the model for drifting too far from the model it started as. Without it the policy discovers whatever quirk of the reward model gives a high score and rides it — the classic failure is that it stops writing sensible English altogether. That is reward hacking, and the next slide is about living with it. Pace this at ~3 minutes; if you are cutting, cut the NEXT slide before this one — but see the note there, because the failure modes are the part a clinical audience actually needs.

--- slide 19 · layout: Blank ---
RL · 10 OF 11 · RLHF, HONESTLY · 💬 PAIR
What RLHF buys — and what it breaks
[image: Picture 4]
MASS SPEC · BUYS: a base model just CONTINUES your text — ask for a reference interval and it writes more questions. After SFT + RLHF it answers and hedges. BREAKS: it optimises what SCORES well, not what is true — sycophancy, and no fix for hallucination. 💬 Dangerous where in YOUR lab?
NOTES:
  OPTIONAL DEPTH (instructor, 2026-09-14) — and of the two RLHF slides this is the one I would fight to keep, because it is the only place in the course that explains WHY a chatbot behaves the way it does, and because every failure mode on it is one they will meet. ~4 minutes including the pair prompt. LEFT, WHAT IT BUYS, and the contrast is the whole point. Put the same question to a BASE model — one that has only been pretrained on the next-token game from the start of the hour — and it does not answer you at all: it CONTINUES your text, producing more questions of the same shape, because continuing text is literally the only thing it was ever trained to do. Read the base-model box aloud; it always gets a laugh, and it makes stages 2 and 3 of the pipeline concrete in a way no description does. After supervised fine-tuning and RLHF, the same model answers the question, hedges where hedging is honest (collection time, assay), and defers to the local range. That transformation is what those two stages bought. RIGHT, WHAT IT BREAKS — three failure modes, all direct consequences of the mechanism on the previous slide. (1) REWARD HACKING: the policy optimises what the reward model SCORES, and what raters score well is not the same as what is true. Long, confident, agreeable answers score well, so you get long, confident, agreeable answers. This is the same reward hacking the Quiz 13 Q1 key warns about — call that forward explicitly, because it lands harder when they see it bite a system as expensive as ChatGPT. (2) SYCOPHANCY: tell the model it is wrong and it will often fold — 'you're right, my mistake' — whether or not you were right. Give the example on screen. In a lab this is genuinely dangerous: the assistant will agree with a confident senior colleague who has misremembered a cutoff. (3) IT DOES NOT FIX HALLUCINATION, and this is the one to say slowly: preference tuning rewards answers that LOOK good to a rater, and a fluent fabrication looks better than an honest 'I don't know'. RLHF can make hallucination MORE confident, not less. So nothing about the guardrails from the first part of the hour changes — ground the answer in your own documents (RAG, two stops from now) and require a human to sign off every number and citation. RUN THE PAIR PROMPT: 60 seconds — where in your workflow would an assistant that agrees with you be dangerous? Harvest two. Typical answers: confirming a result you already believe, agreeing that a borderline QC is acceptable, endorsing a cutoff someone half-remembers. Then the closing you-do.

--- slide 20 · layout: Blank ---
RL · 11 OF 11 · YOUR TURN (YOU-DO = Q1)
Your turn — formulate something from YOUR lab as RL
[image: Picture 4]
CHECKPOINT · Your turn → Quiz 13, Q1, open-ended. Take a decision your lab makes over and over and write it as RL: the AGENT, its ACTIONS, the STATE it observes, the REWARD that says “better”. Then the honest check — is your state MARKOV? Seeds and pitfalls in the notes.
NOTES:
  YOU-DO = Quiz 13 Q1 — and as of 2026-09-11 it is deliberately OPEN-ENDED (instructor request): rather than labelling a scenario I hand them, the room formulates a problem of their OWN as RL. This is where the mass-spec anchor for the whole RL block lives — the mechanics were taught on gridworld, bandits and an aliased corridor precisely so that this slide is the moment it comes home. There is no single right answer; you are marking whether the four roles are coherent and whether the reward actually encodes what they want. RUN IT: two minutes writing alone, then harvest two or three out loud and stress-test them against the blank loop on screen. SEED IDEAS if the room is quiet — offer them only after people have tried: auto-tuning an instrument method (agent = the tuning routine, actions = the setting changes it may make, state = current settings plus the last spectrum's quality metrics, reward = a quality score); scheduling QC or calibration runs (actions = run QC now or wait, reward = drift caught early minus instrument time spent); deciding which flagged sample to re-run (actions = which to re-run, reward = confirmed problems found per re-run); adaptive acquisition, choosing the next precursor to fragment (reward = new confident identifications). THE THREE PITFALLS TO LISTEN FOR, and they are the teaching content of the debrief. (1) It is really SUPERVISED learning in disguise: if they already have the right answer for every case, they do not need RL — they need a classifier. RL earns its keep only when nobody can label the correct action and feedback arrives after the fact. (2) The REWARD encodes the wrong thing: 'maximise identifications' will happily be gamed by loosening the FDR; a reward has to include the cost. This is reward hacking, and it is the single commonest way an RL project goes wrong. (3) The STATE is not MARKOV — the callback to the requirement slide, and the reason that slide exists. A single instrument reading is usually not enough; the reading plus its drift rate plus where in the run you are usually is. Ask at least one volunteer directly: is your state enough on its own to decide from? Close by noting that Lecture 14's agents run this same loop with a human supplying the reward.

--- slide 21 · layout: Blank ---
USING AN LLM · 1 OF 3 · THE THREE OPTIONS · I-DO
Prompting vs RAG vs fine-tuning
[image: Picture 4]
MASS SPEC · You almost never train an LLM — you USE one, three ways. PROMPTING: just ask, with instructions/examples in the prompt (general task, no private data). RAG: retrieve YOUR documents at query time and put them in the prompt (answers must cite current/private docs). FINE-TUNING: further-train on your labeled examples (a consistent new style/skill, many examples).
NOTES:
  The practical stop: how to put an LLM to work without training one. Three options, cheapest first, and as of 2026-09-12 (instructor request) each one carries a CONCRETE example in the right-hand column, so the chart is three worked cases rather than three definitions. Walk the rows left to right. PROMPTING — just ask, putting instructions and a few examples straight in the prompt; best for a general task with no private data, and there is nothing to build. THE EXAMPLE: “Rewrite this method section in plain English for patients.” Nothing in that task needs your data or your documents — it is general language competence, so paste it in and you are done. RAG (retrieval-augmented generation) — at query time, retrieve YOUR relevant documents and put them in the prompt so the model answers FROM them; best when the answer must cite current or private material, and it is also the main hallucination guardrail from earlier in the hour. THE EXAMPLE: “What is today's acceptance criterion for the cortisol assay?” The word doing the work is TODAY'S — the retriever pulls the current SOP chunk and the model answers from it, with a citation, so when the SOP changes next month the answer changes with it and nothing is retrained. FINE-TUNING — further-train the model on your own labelled examples; reserve it for a consistent new skill or style where you also have MANY examples, because it is much the most work. THE EXAMPLE: “Turn this instrument error log into a plain-language cause”, learned from 4,000 past logs. Note what is different about it — you are not retrieving a fact, you are teaching a SKILL the base model does not have, and the skill ends up in the weights. Contrast the three out loud, because the distinction is the whole point: prompting changes the request, RAG changes what the model can SEE at query time, fine-tuning changes the model itself. The footer is the decision rule to leave them with: try prompting first, reach for RAG when the answer has to be current or private, fine-tune last. Next slide opens the RAG box, then the room picks for a scenario of its own (Quiz 13 Q2) — and note that Q2's answer is fine-tuning for a DIFFERENT reason (house report style from 2,000 reports), so it forces a real discrimination rather than a pattern-match on this slide's example.

--- slide 22 · layout: Blank ---
USING AN LLM · 2 OF 3 · RAG, OPENED UP · I-DO
Inside RAG: five parts turn your docs into grounded answers
[image: Picture 4]
MASS SPEC · RAG is five parts, not magic. BUILD once: (1) CHUNK your documents into passages; (2) EMBED each chunk into a vector (meaning → a point in space, the embeddings of Lectures 7–8); (3) STORE them in a knowledge base. ANSWER each query: (4) embed the question and RETRIEVE the nearest chunks (a similarity search — attention's same dot-product 'closeness'); (5) GENERATE — the LLM answers FROM those chunks, with a citation. So 'today's cortisol calibration?' pulls the cortisol-log chunk — the model answers from the CURRENT log, not memory.
NOTES:
  Open the RAG black box, because the room meets it as a guardrail (Q3) and a deployment choice (Q2). Five components, two phases. BUILD THE KNOWLEDGE BASE (offline, once): (1) CHUNKING — split your SOPs, logs, and papers into bite-sized passages so a retrieved piece is specific and fits in the prompt; (2) EMBEDDING — run each chunk through an embedding model that maps text to a vector positioned so passages with similar MEANING land near each other (the 'embedding = a point in space' idea from Lectures 7–8, now for whole passages, not tokens); (3) the VECTOR STORE / knowledge base — keep those chunk vectors indexed for fast lookup. ANSWER A QUESTION (online, every query): (4) RETRIEVAL — embed the question the same way and find the k nearest chunk vectors by similarity (cosine / dot-product 'closeness', the same score attention uses; approximate-nearest-neighbour indexes make this instant over millions of chunks); (5) GENERATION — paste the retrieved chunks plus the question into the prompt and let the LLM answer grounded in them, ideally citing the source chunk. Anchor to say aloud: ask 'today's cortisol calibration?' and the retriever returns the cortisol calibration-log chunk because its embedding is nearest — so the model answers from the CURRENT log, not memory. That grounding is exactly why RAG keeps answers current (Q2) and curbs hallucination (Q3). Honest limits: retrieval quality is everything (bad chunks → bad answer), and chunking / embedding choices matter. Next slide the room picks when to use each option.

--- slide 23 · layout: Blank ---
USING AN LLM · 3 OF 3 · YOUR TURN (YOU-DO = Q2)
Your turn — prompting, RAG, or fine-tuning?
[image: Picture 4]
CHECKPOINT · Your turn → Quiz 13, Q2. You want the assistant to ALWAYS format results in your lab's exact report STYLE — same structure every time — and you have 2,000 past reports as examples. Prompting, RAG, or fine-tuning? In one sentence, why? Worked answer in the notes.
NOTES:
  YOU-DO = Quiz 13 Q2 (pick among prompting / RAG / fine-tuning and justify — and note the answer is DIFFERENT from the I-do's RAG example, so it forces a real discrimination, not a pattern-match). Answer (identical to the Quiz 13 Q2 key): FINE-TUNING. Why (any equivalent): you want a consistent new STYLE applied every time and you have MANY labeled examples (2,000 past reports) — exactly the 'consistent new style/skill + many examples' row of the chart, so bake the style into the weights by fine-tuning. RAG is wrong here because the task isn't retrieving changing FACTS (the report content varies but the STYLE is fixed); plain prompting is wrong because a few in-prompt examples won't reliably enforce one house style across every report. Contrast the I-do (a report that must quote today's SOP → RAG, because THAT was about changing facts). Take one answer with its reason, then have them fill Q2.

--- slide 24 · layout: Blank ---
LECTURE 13 · PART FOUR
The landscape tour
five landmark MS / biology tools, each in one frame: problem → the architecture you now know → impact
NOTES:
  The payoff of the whole course. Five landmark tools from mass spectrometry and structural biology, each on one slide in the SAME frame: the PROBLEM it solves, the ARCHITECTURE it uses — which is one you now know from Lectures 5–11 — and its IMPACT. The through-line to keep saying out loud: these are not exotic new inventions; they are the transformer, the CNN, and the attention mechanism you already understand, pointed at our data. As we go, the room is collecting what they need for the spoken transfer exercise (tool → problem → architecture), which we run out loud at the end of the tour — it is no longer a quiz item. 💬 Keep the outcomes' pair prompt in your pocket: which of these is closest to a problem in your lab?

--- slide 25 · layout: Blank ---
TOUR · 1 OF 5 · DE NOVO SEQUENCING
Casanovo / DeepNovo — sequencing without a database
[image: Picture 4]
MASS SPEC · Problem → architecture → impact → THE PAPER. A TRANSFORMER reads an MS/MS spectrum and writes the peptide — the translation setup of Lectures 7–8. On the nine-species benchmark: 0.81 average precision against 0.74 / 0.70 / 0.58. And it names peptides in NO database.
NOTES:
  Tour stop one, and the cleanest callback. De novo peptide sequencing means reading the amino-acid sequence straight from an MS/MS spectrum WITHOUT matching a database. Casanovo frames it as TRANSLATION: the spectrum is the source sentence, the peptide the target, and a transformer encoder–decoder — the architecture assembled in Lecture 8 — translates one into the other. THE PAPER (instructor, 2026-09-11: every tour stop now carries its citation and its headline result): Yilmaz M, Fondrie WE, Bittremieux W, et al., 'Sequence-to-sequence translation from mass spectra to peptides with a transformer model', Nature Communications 15:6427 (2024), doi:10.1038/s41467-024-49731-x. It is CC BY 4.0, so it is free for anyone in the room to read tonight — say so. THE RESULT on screen is the paper's Fig. 2a benchmark: average peptide-level precision on the nine-species benchmark, Casanovo 0.81 against PointNovo 0.74, DeepNovo 0.70 and Novor 0.58. Two further numbers from the paper worth reading off the cards: it was trained on 30 million labelled spectra from MassIVE-KB, and training on that larger, higher-quality set lifts average precision from 0.81 to 0.95. NOTE ON THE FIGURE: the chart is redrawn by the course from the values the paper reports, not a copy of the journal's artwork — a Nature multi-panel figure is illegible on a projector, and only two of our five papers are openly licensed, so all five panels are drawn the same way for consistency. The citation strip is on every one. Impact: because no reference database is needed, it identifies what databases miss — novel variants, mutations, immunopeptides. Frame: problem → architecture you now know → impact → paper.

--- slide 26 · layout: Blank ---
TOUR · 2 OF 5 · SPECTRUM / RT PREDICTION
Prosit / AlphaPeptDeep — predicting spectra to rescore IDs
[image: Picture 4]
MASS SPEC · Problem → architecture → impact → THE PAPER. A deep SEQUENCE MODEL predicts a peptide's fragment spectrum and retention time from its sequence. Trained on 550,000 peptides and 21 million spectra, it rescores search hits to give more IDs at >10× lower FDR.
NOTES:
  Stop two runs the arrow the other way: given a peptide SEQUENCE, predict what its fragment spectrum and retention time SHOULD look like. Prosit does this with a deep sequence model — an RNN/LSTM with attention (Lecture 7); AlphaPeptDeep does the same job with a transformer (Lecture 8). THE PAPER: Gessulat S, Schmidt T, Zolg DP, et al., 'Prosit: proteome-wide prediction of peptide tandem mass spectra by deep learning', Nature Methods 16:509–518 (2019), doi:10.1038/s41592-019-0426-7. THE NUMBERS on screen are the paper's own: they extended the ProteomeTools synthetic library to 550,000 tryptic peptides and 21 million high-quality tandem mass spectra — that scale is the story, because it is a training set nobody had before — and integrating Prosit into database search pipelines 'led to more identifications at >10× lower false discovery rates'. Worth quoting the paper's boldest line verbatim: the predictions 'exceed the quality of the experimental data'. Why predict a spectrum you could measure? Because comparing the observed spectrum with the PREDICTED one is a powerful extra score: it RESCORES search hits, promoting real identifications and rejecting false ones, so you get more confident IDs at the same FDR. The same predicted-library idea underpins DIA analysis — which is the next stop. This paper is subscription-access, so the figure here is drawn by the course from the numbers it states; the citation is on the slide for anyone who wants the original.

--- slide 27 · layout: Blank ---
TOUR · 3 OF 5 · DIA ANALYSIS · 💬 PREDICT
DIA-NN — untangling data-independent acquisition
[image: Picture 4]
MASS SPEC · 💬 Predict the architecture before I reveal it. In DIA runs many peptides fragment at once, so signals overlap. DIA-NN scores which precursors are really there with an ensemble of FEED-FORWARD NEURAL NETS — the plain MLP from Lectures 1–4. From a half-hour gradient it identifies more than 35,000 precursors.
NOTES:
  Stop three, and a quick mid-tour interactive beat: before you reveal the architecture, ask the room to PREDICT which family it is from the problem alone — then reveal the MLP, which is always a surprise. Data-independent acquisition fragments wide m/z windows, so one spectrum is a jumble of co-eluting peptides: powerful for throughput, hard to deconvolve. THE PAPER: Demichev V, Messner CB, Vernardis SI, Lilley KS, Ralser M, 'DIA-NN: neural networks and interference correction enable deep proteome coverage in high throughput', Nature Methods 17:41–44 (2020), doi:10.1038/s41592-019-0638-x. THE EXPERIMENT, left panel, described as the paper ran it (Fig. 1b): technical repeat injections of a HeLa tryptic digest on a Q Exactive HF, gradients of 0.5, 1, 2 and 4 hours, precursor identifications plotted against FDR, and DIA-NN compared head-to-head with Spectronaut and OpenSWATH. THE HEADLINE NUMBER, right: from a HALF-HOUR gradient DIA-NN consistently identified over 35,000 precursors — which, as the paper points out, is more than was achieved a few years earlier with two-hour nanoflow gradients. That is the throughput story in one number. I have deliberately NOT drawn a fake version of their ID-vs-FDR curve — the panel names what was measured and gives the one number the text states, so nothing on screen is invented. Impact: DIA-NN made DIA robust and routine at scale, thousands of proteins per run, which is why it is a workhorse in proteomics cores. And the teaching point to say out loud: the SIMPLEST architecture in the course is behind one of the field's biggest tools. Match the model to the job, not to the hype.

--- slide 28 · layout: Blank ---
TOUR · 4 OF 5 · A CLINICAL WIN
DRIAMS — antibiotic resistance from MALDI-TOF
[image: Picture 4]
MASS SPEC · Problem → architecture → impact → THE PAPER. Call resistance straight from the routine clinical MALDI-TOF spectrum with a 1D CNN — the Lab 2 model. AUROC 0.80 (S. aureus / oxacillin), 0.74 (E. coli and K. pneumoniae / ceftriaxone). In 63 patients: 9 treatments changed, 8 for the better.
NOTES:
  Stop four, and the clinical one — this is the tour stop closest to the room's own bench, and the dataset behind Lab 2. THE PROBLEM: predict antibiotic resistance directly from the MALDI-TOF spectrum a clinical lab already acquires to identify the organism, instead of waiting up to 72 hours for culture-based susceptibility testing. THE PAPER: Weis C, Cuénod A, Rieck B, et al., 'Direct antimicrobial resistance prediction from clinical MALDI-TOF mass spectra using machine learning', Nature Medicine 28:164–174 (2022), doi:10.1038/s41591-021-01619-9. THE SCALE, top right: they built and released DRIAMS — more than 300,000 mass spectra with more than 750,000 linked antimicrobial-resistance phenotypes from four medical institutions. Say that slowly; a public clinical dataset at that scale is itself a contribution, and it is the data Lab 2 uses. THE RESULT, the bars: validation AUROC of 0.80 for Staphylococcus aureus with oxacillin, and 0.74 each for Escherichia coli and Klebsiella pneumoniae with ceftriaxone. Point at the dashed line: 0.5 is a coin flip, so these are real but far from perfect — and that honesty matters, because it is exactly the AUROC-versus-AUPRC conversation from Lecture 10 on a genuinely imbalanced clinical problem. THE PART THAT LANDS, bottom right: a retrospective study of 63 patients found that using this approach would have changed clinical treatment in nine cases, and in eight of those nine (89%) the change would have been beneficial. That is what a 0.80 AUROC buys in a real workflow. Architecture: framed here as a 1D CNN over the spectrum — the Lecture 5 model they built in Lab 2. Subscription paper, so the chart is the course's, drawn from the numbers the paper reports.

--- slide 29 · layout: Blank ---
TOUR · 5 OF 5 · THE FLAGSHIP
AlphaFold — protein structure from sequence
[image: Picture 4]
CASP14 accuracy as reported in Jumper et al., Nature 596:583–589 (2021), CC BY 4.0 — chart redrawn by the course
MASS SPEC · Problem → architecture → impact → THE PAPER. Predict a protein's 3D structure from its sequence using attention — the Evoformer, Lectures 7–8. At CASP14 its median backbone error was 0.96 Å, about an atom's width, against 2.8 Å for the next best method.
NOTES:
  The flagship, and the same frame. PROBLEM: predict a protein's 3D structure from its amino-acid sequence — the decades-old folding problem. ARCHITECTURE: at its heart AlphaFold is attention — a specialised transformer, the Evoformer, reasoning over the sequence and its evolutionary relatives — the exact mechanism from Lectures 7–8. THE PAPER: Jumper J, Evans R, Pritzel A, et al., 'Highly accurate protein structure prediction with AlphaFold', Nature 596:583–589 (2021), doi:10.1038/s41586-021-03819-2, CC BY 4.0 — also free to read. THE RESULT, and it is the number that ended the argument: in the CASP14 blind assessment AlphaFold achieved a median backbone accuracy of 0.96 Å r.m.s.d.95 with a 95% confidence interval of 0.85–1.16 Å, while the next best method managed 2.8 Å. Give the room the intuition for 0.96 Å: that is roughly the width of an atom — the prediction is as close to the truth as the experiment itself. Explain r.m.s.d.95 in one clause if asked: the average deviation of the backbone carbons over the best-matching 95% of residues. IMPACT: 200 million-plus predicted structures released, structural biology and drug discovery reshaped, and a share of the 2024 Nobel Prize in Chemistry. The message to leave the room with: the architecture that folds proteins is the same attention you now understand, pointed at sequences and scaled up.

--- slide 30 · layout: Blank ---
TOUR · THE WHOLE TOUR · I-DO · 💬 PAIR
The tour on one page
[image: Picture 4]
MASS SPEC · Five tools, one frame — and five papers you can read tonight. Casanovo → de novo sequencing → Transformer. Prosit → spectrum + RT → sequence model. DIA-NN → DIA ID + quant → MLP. DRIAMS → resistance from MALDI-TOF → 1D CNN. AlphaFold → structure → attention. 💬 Which is closest to a problem in YOUR lab?
NOTES:
  The whole tour on one page — the single chart the closing transfer exercise is answerable from (rule 3). Walk the five rows once more by architecture family so the pattern lands: Casanovo = Transformer used as translation (Lecture 8); Prosit = a deep sequence model, RNN/LSTM + attention (Lecture 7); DIA-NN = a feed-forward MLP (Lectures 1–4); DRIAMS = a 1D CNN spectrum classifier (Lecture 5); AlphaFold = attention / transformer (Lectures 7–8). Every one is an architecture the room now owns, aimed at a real problem, with a concrete impact. Run the outcomes' 💬 pair prompt here: which of these five is closest to a problem in your own lab, and which architecture would it use? Harvest two or three, then the room does the formal match next slide, out loud — it is a do-it-together, not a quiz item.

--- slide 31 · layout: Blank ---
TOUR · PLACE A NEW TOOL · 💬 DO-IT-TOGETHER
Together — place a tool you've never seen
[image: Picture 4]
PAIR · 💬 Out loud, together — not on the quiz. Using the five-tool map: (a) a tool calls bacterial SPECIES from a MALDI-TOF spectrum → which architecture family, most like which landmark? (b) a tool predicts a peptide's fragment spectrum + RT from its sequence → which family, like which landmark? (c) Casanovo and AlphaFold share ONE family — which, and what do their problems have in common?
NOTES:
  CHANGED 2026-09-11 (instructor): Quiz 13's old Q1 — this transfer item — was removed from the sheet, so this slide is no longer a you-do. Keep the beat, run it OUT LOUD as a do-it-together / pair prompt: it is the highest-value 3 minutes of the tour, because it is the difference between recalling five names and being able to place the sixth tool they meet next month. Nothing to write; take answers from the floor. ANSWERS. (a) A spectrum-in, one-label-out classifier is a 1D CNN, most like DRIAMS — same architecture, a species call instead of R/S. (b) Sequence-in, predicted spectrum + RT out is a deep SEQUENCE MODEL (RNN/attention, or a transformer), most like Prosit. (c) Casanovo (spectrum → peptide) and AlphaFold (sequence → 3D fold) both use a TRANSFORMER / ATTENTION; what their problems share is LONG-RANGE dependency across a sequence — distant peaks or residues have to inform each other, which is exactly what attention captures and exactly what a CNN, seeing only local windows, cannot. Every answer is reachable from the one-page tour chart on the previous slide, but the room has to REASON, not recall. Take one full answer for each, then move to the recap.

--- slide 32 · layout: Blank ---
LECTURE 13 · WHAT YOU CAN DO NOW
You can now…
•  Explain LLM training in pictures — pretraining → SFT → RLHF — how RLHF is built, and what it breaks (Q3)
•  Describe RL properly — loop, state & policy, Markov, explore vs exploit, discounting (Q4, Q5)
•  Formulate a repeated decision in your own lab as reinforcement learning (Q1)
•  Choose among prompting, RAG, and fine-tuning, and cite the paper behind each tool (Q2)
You have worked every item on Quiz 13 live — your own RL formulation (Q1), prompting/RAG/fine-tuning (Q2), the untrustworthy claim (Q3), explore vs exploit (Q4), the discounted return (Q5). Keep the sheet.
NOTES:
  Recap = the Lecture 13 outcomes, each exercised this hour. The RL line is deliberately stronger than it was (instructor, 2026-09-11): the room can now name STATE and POLICY, state the Markov requirement and spot when it fails, apply ε-greedy, and discount a late reward back to the move that set it up — all taught on the classic examples. The separate bullet for Q1 is the one that matters most for their day job: they have formulated a repeated decision from their own lab as RL and pressure-tested whether its state is sufficient. The tour line now says CITE, because every tour slide carries its paper and that paper's headline number — encourage them to read the two that are CC BY tonight (Casanovo in Nature Communications, AlphaFold in Nature). If you dropped optional RL slides, drop the matching clauses and say which quiz items stay blank.

--- slide 33 · layout: Blank ---
LECTURE 13 · NEXT
Next hour — Agentic AI, demo-driven
•  From chatbot to agent: an LLM + tools + a reason→act loop (ReAct) — a direct callback to today's RL feedback
•  Follow along on your own free-tier bot: prompt for explicit reason → act → check steps
•  Instructor live demo: an 'MS analysis assistant' reading a QC CSV, plotting, flagging anomalies, drafting a summary
•  Where agents fit in the lab — and the guardrails each needs (human review, restricted tools, logging)
CHECKPOINT · 2 minutes to finish Quiz 13 solo → we mark it together as Lecture 14 opens. Notice the thread: an agent's reason→act loop IS today's RL loop, and today's hallucination guardrails are what keep that agent honest next hour.
NOTES:
  Hand the last minutes to finishing Quiz 13, then sell Lecture 14. The through-line: an AGENT is an LLM given tools and a reason→act loop (ReAct), and the two ideas from today carry straight over — reinforcement-style feedback is how such behaviors get shaped, and the hallucination guardrails (grounding, human review, restricted tools, logging) are exactly what keep an agent safe in a lab workflow. Next hour we go demo-driven: participants run a small reason→act→check exercise on their own free-tier chatbot, then watch a live "MS analysis assistant" read a QC CSV, plot it, flag anomalies, and draft a summary — each step narrated. Then Colab boots for the capstone and we mark Quiz 13 together.
